# Lead CRM Environment Variables
# Copy this file to .env.local and fill in the values

# ===========================================
# DATABASE (PostgreSQL) - REQUIRED
# ===========================================
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
#
# For local development with Docker:
# DATABASE_URL=postgresql://lead_crm_user:devpassword@localhost:5433/lead_crm
#
# For connecting to production via SSH tunnel:
# DATABASE_URL=postgresql://lead_crm_user:PASSWORD@localhost:5432/lead_crm
#
DATABASE_URL=

# ===========================================
# AUTHENTICATION - REQUIRED
# ===========================================
# Secret key for JWT signing (min 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=

# ===========================================
# DEPRECATED - DO NOT USE
# ===========================================
# The following Supabase variables are NO LONGER USED
# We migrated to self-hosted PostgreSQL
#
# NEXT_PUBLIC_SUPABASE_URL=      # ❌ NOT USED
# NEXT_PUBLIC_SUPABASE_ANON_KEY= # ❌ NOT USED
# SUPABASE_SERVICE_ROLE_KEY=     # ❌ NOT USED
